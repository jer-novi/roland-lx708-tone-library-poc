spring:
  datasource:
    url: jdbc:h2:mem:tonelib;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

app:
  seed:
    enabled: true
  cors:
    allowed-origins: http://localhost:3000
  wikipedia:
    base-url: http://localhost:9999
    bulk-delay-ms: 0
